# FacDiggerNN 项目关键问题与修复复盘

> **持续维护的复盘档案**：记录项目开发过程中真实发生或在工程验证、研究审查中真实暴露
> 的关键问题，以及最终采用的修复方案。本文面向面试复盘，不替代[开发文档](开发文档.md)
> 或[实验设计文档](实验设计文档.md)，不把预防性设计伪装成生产事故，也不把尚未完成的
> 实验描述成已经得到的研究结论。

最后更新：2026-08-04

---

## 1. 如何使用这份文档

面试时不要从目录结构开始背诵项目。更有效的讲法是：

1. 用 30 秒说明项目目标；
2. 选择一个与岗位最相关的关键问题；
3. 按“现象 → 根因 → 方案权衡 → 最终设计 → 验证 → 局限”展开；
4. 最后说明如果数据、算力或时间更充足，下一步怎样升级。

本文给问题标注三种证据等级：

| 标记 | 含义 |
|---|---|
| `实际运行故障` | 在真实命令或真实 EODHD 数据上已经触发 |
| `工程验证暴露` | 在兼容性探针、单元测试、集成测试或跨平台运行中发现 |
| `研究审查发现` | 代码可以运行，但结果可信度检查发现不能直接用于正式结论 |

这一区分很重要。面试时可以坦诚地说：“这个问题不是线上事故，而是我在正式实验前通过门禁主动发现并阻断的。”这通常比夸大故障更能体现工程判断。

---

## 2. 项目的一句话介绍

FacDiggerNN 是一个面向美股日频横截面选股的机器学习因子研究工具。它以 EODHD 为首个数据源，以 PatchTST 为时序编码器，比较：

- `E0`：LightGBM / MLP 传统基线；
- `E1`：随机初始化 PatchTST；
- `E2`：从 IBM ETTh1 PatchTST 迁移；
- `E3`：ETTh1 迁移后继续做金融域 masked-patch 预训练。

项目重点不是“训练出一个模型”，而是把数据、点时语义、防泄漏训练、权重迁移、统一评价、Walk-forward、holdout 冻结、checkpoint 回放和无标签因子导出连成一条可审计链路。

---

## 3. 项目演进和问题地图

| 阶段 | 主要交付 | 关键问题 |
|---|---|---|
| M0 | 环境检查、PatchTST 兼容性探针 | 外部 checkpoint 配置和权重命名不兼容 |
| M1 | EODHD → 标准 Parquet → 不可变快照 | 数据源耦合、API 密钥与配额、点时数据契约 |
| M2 | E0 和统一评价 | LightGBM 原生运行库冲突、评价口径统一 |
| M3 | E1 和恢复训练 | 缺失值 mask、训练中断后的精确恢复 |
| M4 | E2 迁移学习 | `strict=False` 假成功、冻结阶段是否真的生效 |
| M5 | E3 金融域预训练 | 缺失填充值进入重建损失、无监督预训练泄漏未来分布 |
| M6 | Walk-forward 与冻结研究 | outer validation 泄漏、重叠收益统计、中性化数据不足 |
| M7–M8 | 回放、因子导出、独立评价 | checkpoint/数据漂移、target 泄漏到生产因子 |
| 正式实验准备 | 历史动态股票池和退市处理 | 存活偏差、退市收益缺失、ticker/ISIN 冲突 |

---

## 4. 问题总表

| ID | 问题 | 证据等级 | 核心修复 |
|---|---|---|---|
| DATA-01 | EODHD 同一 ISIN 产生重复日线主键 | 实际运行故障 | 衍生品过滤 + ticker alias 确定性合并 |
| DATA-06 | EODHD 返回零价占位和不一致 OHLC | 实际运行故障 | 拒绝伪造修复，丢弃无效观测并显式保留缺失 session |
| DATA-07 | 少量闭市日 bars 污染全市场日历和 ADV20 | 研究审查发现 | 显式 US regular-session 日历 + 闭市日过滤 + 完整覆盖门禁 |
| DATA-08 | ticker 复用、错误 ISIN alias 和复权断裂产生万倍跳变 | 研究审查发现 | 合并前冲突检测 + 相邻 session 复权价门禁 + 整身份隔离 |
| DATA-09 | 公司行动 alias 重复和同日经济条款冲突 | 研究审查发现 | 经济语义去重；冲突事件整组 fail closed |
| DATA-10 | old/current alias 合并后仍被错误标记为退市 | 研究审查发现 | active alias 优先生命周期 + 从合并后 universe 生成退市事件 |
| DATA-11 | 旧 bronze 可通过基础契约并被误用于新实验 | 工程验证暴露 | manifest 质量证明 + validate/snapshot/preflight 三层拒绝 |
| DATA-02 | 当前活跃股票池产生存活偏差和历史前视偏差 | 研究审查发现 | active + delisted 候选，按历史日 ADV20 动态排名 |
| DATA-03 | 个股交易日推进让 `t+5` 在停牌时错位 | 研究审查发现 | 使用全市场 session 日历定义标签终点 |
| DATA-04 | 缺少真实退市终值导致跨退市样本静默丢失 | 研究审查发现 | 显式、可审计的交易所惩罚插值 |
| DATA-05 | 数据供应商格式容易渗透到训练代码 | 工程验证暴露 | provider adapter + 标准表契约 |
| OPS-01 | API token、缓存和付费配额存在安全与成本风险 | 工程验证暴露 | 环境变量、脱敏缓存、持久化加权预算 |
| REPRO-01 | 原始数据或配置变化后仍可能误用旧实验 | 工程验证暴露 | 不可变快照、内容哈希、原子写入 |
| MODEL-01 | IBM 旧版 PatchTST checkpoint 不能直接安全加载 | 工程验证暴露 | 配置规范化、两段权重迁移、参数量门禁 |
| MODEL-02 | `strict=False` 可能让随机模型伪装成迁移成功 | 工程验证暴露 | mismatch allowlist、shape 审计、指纹一致性 |
| MODEL-03 | 缺失值补零会污染监督聚合和 masked reconstruction | 工程验证暴露 | observed mask 贯穿窗口、patch 和 loss |
| TRAIN-01 | 使用 outer validation early stopping 会污染实验结论 | 研究审查发现 | Train 内 inner selection + label overlap purge |
| TRAIN-02 | 只保存模型权重无法精确恢复训练 | 工程验证暴露 | optimizer/scheduler/scaler/RNG/sampler 全状态 checkpoint |
| TRAIN-03 | “冻结 encoder”可能因配置错误而没有真的冻结 | 工程验证暴露 | optimizer step 前后参数/缓冲区指纹审计 |
| TRAIN-07 | 多 split 重复物化全量特征导致 E3 内存放大 | 工程验证暴露 | 共享只读 feature store + 按 split 保存轻量索引 |
| TRAIN-08 | 16 GB 目标机全流程仍因快照和 E0 峰值 OOM | 实际运行故障 | 窗口裁剪、分阶段释放、Float32 流式统计和 mmap |
| TRAIN-09 | Huber 回归目标与横截面排序研究问题不一致 | 研究审查发现 | 日期分组采样 + 排序相关性 surrogate / LambdaRank |
| SYS-01 | LightGBM 与 Polars/PyTorch 的 OpenMP 运行库可能冲突 | 工程验证暴露 | LightGBM 训练和推理进程隔离 |
| ENV-01 | 虚拟环境绑定临时解释器后无法复用 | 实际运行故障 | 从稳定 Python 解释器按 lockfile 重建环境 |
| EVAL-01 | 缺少点时行业、市值时不能声称完成中性化 | 研究审查发现 | 中性结果置空、记录原因、工程模式关闭硬门禁 |
| EVAL-02 | 重叠收益让普通 t 检验过度乐观 | 研究审查发现 | 按日期聚合 seed、HAC 与非重叠样本双检验 |
| EVAL-03 | 只检查 Rank IC 均值方向会把噪声当成证据 | 研究审查发现 | 单侧 HAC + 非重叠稳健性 + Holm 校正 |
| RESEARCH-01 | 多次查看 test 会把 holdout 变成调参集 | 研究审查发现 | validation freeze + 哈希锁定 + 显式解封 |
| RESEARCH-02 | 冻结后直接复用旧模型评价最终 holdout | 研究审查发现 | 截至 validation 末日重建 snapshot 并 final refit |
| INFER-01 | 训练结束后内存预测不能证明 checkpoint 可复现 | 工程验证暴露 | 独立进程回放和多层哈希校验 |
| INFER-02 | 因子导出容易把未来 target 一起带出 | 工程验证暴露 | 独立 inference index，factor 文件禁止 target |

---

## 5. 旗舰案例：EODHD 同一 ISIN 导致重复主键

### 5.1 证据与影响

- 证据等级：`实际运行故障`
- 触发命令：

```bash
uv run facdigger data ingest \
  --config configs/data/eodhd_historical_liquid.yaml
```

- 真实错误：

```text
bars_daily has duplicate keys ['security_id', 'trade_date']
```

- 已观察到的冲突包括：

```text
eodhd:isin:KYG330331201
eodhd:isin:US00258Y1047
eodhd:isin:KYG330321061
```

这个错误由标准表契约在写入 Parquet 前阻断。如果没有唯一键门禁，重复记录会进入 rolling feature、标签和横截面排序，后续结果可能仍能生成，但一个证券会在同一天出现两次，属于“静默污染”。

### 5.2 根因不是单一的“脏数据”

真实数据检查发现存在两类完全不同的冲突。

第一类是 EODHD 元数据的证券类型泄漏：

```text
AACT      Ares Acquisition Corporation II
AACT-WT   Ares Acquisition Corporation II WT
```

二者被标为 `Common Stock`，并共享 `KYG330331201`，但价格显然属于普通股和权证两个不同工具，不能按同一证券合并。

第二类是真实 ticker 迁移或别名：

```text
ABX
ABL
```

二者共享 `US00258Y1047`，EOD endpoint 返回大量重叠历史；同一天的 OHLC 基本相同，但 volume 可能有轻微舍入差异。这一类应该保持证券历史连续，而不能被拆成两个独立公司。

### 5.3 为什么几个直觉方案都不够好

方案一：直接把 `security_id` 改成 ticker。

- 优点：立即消除重复键。
- 问题：ticker 会变化；公司改名或换代码后历史被切断。
- 后果：rolling feature、上市天数、退市判断和训练窗口全部出现人为断点。

方案二：遇到重复行就 `unique(keep="first")`。

- 优点：代码最短。
- 问题：结果取决于 API 顺序和 batch 顺序，不具确定性。
- 问题：可能把权证价格当成普通股，也可能丢掉旧 ticker 独有的早期历史。

方案三：所有相同 ISIN 都无条件合并。

- 优点：保持 ticker 迁移连续。
- 问题：EODHD 的权证、单位和 rights 可能与普通股共享 ISIN 或被错误标型。
- 后果：会把价格量级完全不同的证券拼成一条时间序列。

### 5.4 最终采用的两层修复

第一层：候选发现阶段修正明显的证券类型泄漏。

- 保留 provider 的 `Type` 初筛；
- 再检查名称中的 `warrant`、`unit`、`right`；
- 检查 `-WT`、`-UN`、`-RT` 等衍生代码后缀；
- 对无分隔符的 `W/U/R` 后缀，仅在存在同名基础 ticker peer 时排除，降低误杀普通公司的风险；
- 在 manifest selection audit 中记录排除数量。

第二层：对真正的 ticker alias 合并历史。

- 稳定身份仍优先使用 ISIN；
- 非重叠日期全部保留，避免丢失旧 ticker 独有历史；
- 重叠日期优先当前 active alias；
- 其次优先历史覆盖终点更晚的 alias；
- 最后按 provider symbol 排序，保证确定性；
- batch 内和所有 batch concat 后都执行一次，避免同一 ISIN 跨 250-symbol batch 时再次冲突；
- manifest 记录 `overlapping_alias_rows_resolved`。

### 5.5 验证

- 新增 ticker alias 重叠测试；
- 新增衍生工具过滤测试；
- 原 EODHD adapter 测试全部通过；
- 全项目 72 项测试通过；
- 复用已有 977 个 API 缓存响应恢复真实采集，API 调用继续增长，原重复键没有在相同位置再次触发。

代码证据：

- [`src/facdigger/data/providers/eodhd/universe.py`](../src/facdigger/data/providers/eodhd/universe.py)
- [`src/facdigger/data/providers/eodhd/mapper.py`](../src/facdigger/data/providers/eodhd/mapper.py)
- [`src/facdigger/data/providers/eodhd/provider.py`](../src/facdigger/data/providers/eodhd/provider.py)
- [`tests/unit/data/providers/test_eodhd.py`](../tests/unit/data/providers/test_eodhd.py)

### 5.6 面试讲法

> 我在第一次全量下载 2 万多只美股历史时，标准表唯一键门禁拦住了重复的 `(security_id, trade_date)`。继续看真实 payload 后发现这不是普通 dedup：一部分是 ticker rename，应该合并；另一部分是 EODHD 把权证和 unit 错标成 Common Stock，而且它们还共享 ISIN，绝对不能合并。我最后做了两层设计：先用名称、后缀和 peer ticker 修正衍生品泄漏，再对真正的 alias 保留非重叠历史，并用 active、覆盖终点和 ticker 做确定性重叠选择。这个方案没有退化成用 ticker 当主键，也不会因 API 返回顺序改变结果。

### 5.7 尚存局限

- ISIN 本身也可能存在供应商错误；
- 规则型衍生品识别不能替代完整 security master；
- 最终全量 Parquet 和 selection audit 仍需在采集完成后检查；
- 更成熟的方案是接入带 `effective_from/effective_to` 的 ticker mapping 和 instrument type 主数据。

### 5.8 后续真实故障：零价占位和不一致 OHLC

DATA-01 修复后，采集从 977 个缓存响应继续到 1190 个，随后再次被数据契约阻断：

```text
bars_daily has 24 invalid OHLCV/adjustment rows
```

扫描当时已有缓存，共发现 15 个 symbol、33 行非法观测，主要分为：

1. 全部 OHLC、adjusted close 和 volume 都是 0 的 provider 占位记录；
2. `low > close` 或 `high < close` 的内部不一致 OHLC；
3. 前一版过滤规则漏掉的 `AAGRW`、`ACABW`、`ACII-U`、`ACHR-WS` 等衍生工具。

真实样例：

```text
ACAQ.US  2023-11-06  open=0 high=0 low=0 close=0 adjusted_close=0 volume=0
ACII-U.US 2021-02-12 open=10.95 high=11.25 low=10.895 close=10.85
ACHR-WS.US 2024-06-28 open=0.334 high=0.355 low=0.334 close=0.3725
```

这里没有采用以下“修复”：

- 把 0 价格替换为前值：这会凭空制造可交易价格和成交；
- 强行令 `high=max(open, high, close)`：这会掩盖供应商错误；
- 关闭 OHLCV contract：这会让错误进入特征和标签；
- 遇到坏行删除整只股票：这会不必要地损失其他有效历史。

最终处理分两层：

- 扩展衍生工具识别，覆盖显式 `-U/-WS`，以及 Nasdaq 常见的第五字符 `W/U/R` 和 `_OLD` alias；
- provider 映射前检查 finite、正价格、OHLC 包含关系、非负 volume 和正复权因子；
- 只丢弃非法“观测行”，不修造价格；
- 后续 universe 全市场 session 网格仍保留该日期，并将缺失观测显式表示为 halt/missing；
- manifest 记录 `invalid_eod_rows_dropped`、相关 symbols 和 warning。

这次修复新增 provider placeholder 测试，项目测试总数增至 72。

面试时可概括为：

> 数据清洗不是让 contract 不报错，而是决定错误信息应该怎样进入研究语义。对 0 价占位，我没有 forward-fill 或修改 high/low，而是把它当成“不存在可信 bar”。删除坏观测后，市场 session 仍在 universe grid 中保留，因此下游看到的是显式 missing/halt，而不是一根伪造 K 线。

---

## 6. 从“当前最活跃 100 只”升级为历史动态股票池

### 6.1 证据与问题

- 证据等级：`研究审查发现`
- 早期 paid pilot 从“今天仍 active 的股票”中按当前 200 日平均成交量选 100 只，再回看历史。

工程上它很方便，但研究上有两个偏差：

1. 存活偏差：历史上退市的公司完全不在样本中；
2. 历史前视偏差：用今天的流动性决定多年前的股票池。

这会系统性高估策略表现，尤其会忽略破产、并购和低流动性退出样本。

### 6.2 最终设计

- 候选发现同时读取 active 和 delisted 普通股；
- 候选阶段不使用当前流动性排名；
- 对每只证券恢复上市区间内的全市场 session 网格；
- 仅用截至当日的 trailing ADV20；
- 每个历史交易日独立选取最多 1000 只；
- 记录候选规模、active/delisted 数量、排名规则和 research readiness。

这里的关键拆分是：

```text
候选发现：谁在历史上可能存在
        ↓
点时 eligibility：某只股票在某一天是否可交易、是否足够流动
```

两者不能合并成“先用今天的数据选一批股票，再回测”。

代码证据：

- [`configs/data/eodhd_historical_liquid.yaml`](../configs/data/eodhd_historical_liquid.yaml)
- [`src/facdigger/data/providers/eodhd/universe.py`](../src/facdigger/data/providers/eodhd/universe.py)
- [`src/facdigger/data/providers/eodhd/mapper.py`](../src/facdigger/data/providers/eodhd/mapper.py)

### 6.3 面试讲法

> 最开始的 100 股票 pilot 只用于证明系统能跑通。我没有把它包装成正式回测，因为它先看了今天谁还活着、谁现在最流动，再回到历史。正式协议把 candidate discovery 和 daily eligibility 分开：candidate 纳入 active 和 delisted，daily eligibility 只使用当时能知道的 trailing ADV20。这样工程 pilot 和研究结论的边界是显式的。

---

## 7. 标签终点不能按“个股第 5 根 K 线”推进

### 7.1 证据与问题

- 证据等级：`研究审查发现`
- 目标是预测全市场交易日 `t+5` 的 next-open 到 next-open 超额收益。

如果按每只股票自身的第 5 个后续观测推进，停牌一天的股票会把标签终点移到市场 `t+6`。不同股票的“5 日收益”实际对应不同持有期，横截面不可比，也产生对停牌状态的选择偏差。

### 7.2 最终设计

- 从所有日线构建全市场 session 日历；
- `label_start` 和 `label_end` 通过市场 session 位移得到；
- 股票某个 session 缺少 bar 时，在 universe grid 中显式标记 `is_halted`；
- 标签不能偷偷寻找更晚的一根个股 K 线替代目标日；
- 若窗口跨退市，则进入退市 terminal value 分支。

代码证据：

- [`src/facdigger/labels/forward_return.py`](../src/facdigger/labels/forward_return.py)
- [`src/facdigger/data/providers/eodhd/mapper.py`](../src/facdigger/data/providers/eodhd/mapper.py)
- [`tests/integration/test_dataset_pipeline.py`](../tests/integration/test_dataset_pipeline.py)

### 7.3 面试讲法

> 一个容易忽略的问题是 horizon 到底是“市场过去 5 个 session”还是“这只股票出现 5 根 K 线”。后者遇到停牌会错位。我把 universe 做成证券 × 市场 session 网格，标签终点由全市场日历定义，缺失 bar 明确标成停牌或缺失，不允许向后偷找价格。

---

## 8. 没有真实退市收益时，如何避免静默删除失败样本

### 8.1 证据与问题

- 证据等级：`研究审查发现`
- 当前 EODHD 套餐能提供 delisted 列表，但没有足够可靠的退市原因和 terminal return。

如果一只股票在标签窗口内退市，`label_end` 没有正常价格。最简单的实现会得到 null 并删除样本，而这些样本通常表现更差，删除它们会形成系统性乐观偏差。

### 8.2 最终设计

工程阶段采用明确、版本化的交易所惩罚假设：

| 交易所 | 默认 `delisting_return` |
|---|---:|
| Nasdaq | -55% |
| NYSE / NYSE American | -30% |
| 未知 | -50% |

具体约束：

- 插值表逐行标记 `is_imputed=true`；
- 记录 `imputation_method` 和 exchange；
- source manifest 写入完整政策和警告；
- 标签窗口跨退市时，用最后有效价格乘 `(1 + delisting_return)`；
- 数据仍标记为不满足正式 research readiness；
- 实验文档要求对惩罚参数做敏感性分析。

这不是声称插值“正确”，而是把未知问题从静默删除变成显式、可复核的假设。

代码证据：

- [`src/facdigger/data/providers/eodhd/mapper.py`](../src/facdigger/data/providers/eodhd/mapper.py)
- [`src/facdigger/labels/forward_return.py`](../src/facdigger/labels/forward_return.py)
- [`src/facdigger/data/contracts.py`](../src/facdigger/data/contracts.py)

### 8.3 面试讲法

> 我没有用插值假装获得了真实退市收益。我的目标是先修复“坏样本静默消失”这个更危险的问题：把惩罚假设写进数据行和 manifest，并降低 source readiness。正式研究仍需要 CRSP 一类真实 delisting return；当前结果只能做工程验证和敏感性分析。

---

## 9. 数据源适配：避免把 EODHD 字段写进整个项目

### 9.1 证据与问题

- 证据等级：`工程验证暴露`
- EODHD 使用 `adjusted_close`、`Isin`、`Code`、`Exchange` 等供应商字段；后续更换供应商时字段和语义都会变化。

如果 feature、label 和 model 直接读取这些字段，数据源会成为全项目的结构性依赖。

### 9.2 最终设计

采用三层边界：

```text
EODHD client
  只负责 HTTP、缓存、重试和预算
        ↓
EODHD mapper/provider
  负责供应商字段解释和身份映射
        ↓
标准 Parquet contracts
  bars / universe / corporate_actions / delistings
        ↓
特征、标签、训练、评价
```

所有下游模块只依赖标准表。增加新供应商时，应实现 provider adapter，而不是修改模型代码。

代码证据：

- [`src/facdigger/data/providers/base.py`](../src/facdigger/data/providers/base.py)
- [`src/facdigger/data/providers/registry.py`](../src/facdigger/data/providers/registry.py)
- [`src/facdigger/data/contracts.py`](../src/facdigger/data/contracts.py)
- [`src/facdigger/data/providers/eodhd/`](../src/facdigger/data/providers/eodhd/)

### 9.3 巧妙点

供应商特有字段没有完全丢弃，而是作为审计列保留，例如：

- `provider_symbol`；
- `identity_quality`；
- `exchange_source`；
- `trade_status_quality`；
- `adjustment_basis`。

这样既保持下游接口中立，又没有牺牲问题追踪能力。

---

## 10. API token、缓存和付费配额的三重约束

### 10.1 证据与问题

- 证据等级：`工程验证暴露`
- 全历史候选约 2 万多只，同时请求 EOD、dividend 和 split，调用量约 6.8 万。
- token 如果进入 YAML、cache key、manifest 或异常日志，就可能被提交到 Git。
- 部分 EODHD endpoint 的一次网络请求可能按多次 API call 计费。

### 10.2 最终设计

安全：

- YAML 只写环境变量名称；
- token 存在 `.env.local`，并由 `.gitignore` 排除；
- cache identity 明确移除 `api_token`；
- manifest 只记录 token 来源名，不记录值。

成本：

- 请求前持久化 reserve 调用预算；
- 同时记录 `network_attempts` 和 `api_calls`；
- 支持 bulk endpoint 的 `call_cost`；
- 达到日预算前主动失败；
- 成功响应落盘缓存，重启后复用。

可恢复性：

- cache key 由 path 和排序后的公开参数哈希得到；
- 写缓存先写临时文件再原子 rename；
- 失败重启不会重复下载已有响应。

代码证据：

- [`src/facdigger/data/providers/eodhd/client.py`](../src/facdigger/data/providers/eodhd/client.py)
- [`tests/unit/data/providers/test_eodhd.py`](../tests/unit/data/providers/test_eodhd.py)
- [`.gitignore`](../.gitignore)

### 10.3 面试讲法

> 对全量金融数据，API adapter 不只是 `requests.get`。我把 secret safety、可恢复缓存和加权配额放在同一个 client 边界里。测试会直接读取缓存文件确认 token 不存在；预算在请求前 reserve，所以进程崩溃最多保守多计，不会因为失败重试突破付费上限。

---

## 11. 不可变快照：防止“同一个路径、不同一份数据”

### 11.1 证据与问题

- 证据等级：`工程验证暴露`
- 训练配置可能仍指向同一个 `bars_daily.parquet` 路径，但文件内容已经被重新采集覆盖。
- 仅记录路径无法证明某个 checkpoint 使用了哪一份数据。

### 11.2 最终设计

快照构建时固化：

- 标准化数据；
- features、labels、sample index；
- sample metadata；
- 不含 label/split 的 inference index；
- source manifest；
- 每个文件的 SHA-256；
- dataset ID 和 schema version。

训练 manifest 再记录：

- dataset manifest hash；
- resolved config hash；
- source checkpoint hash；
- 代码/环境信息；
- split 访问审计。

恢复或回放时，哈希不一致直接阻断。

代码证据：

- [`src/facdigger/data/snapshots.py`](../src/facdigger/data/snapshots.py)
- [`src/facdigger/experiments/manifest.py`](../src/facdigger/experiments/manifest.py)
- [`src/facdigger/inference/runner.py`](../src/facdigger/inference/runner.py)

### 11.3 面试讲法

> 我不把“配置文件路径相同”当作可复现。数据、配置和 checkpoint 都有内容哈希，run manifest 记录三者的绑定关系。这样可以回答：这个分数究竟来自哪份原始 source manifest、哪份快照、哪个 checkpoint，而不是只能说“应该是那个目录”。

---

## 12. IBM PatchTST checkpoint 的跨版本迁移

### 12.1 证据与问题

- 证据等级：`工程验证暴露`
- IBM checkpoint 使用的 PatchTST 配置字段和 state key 来自较早实现；
- 当前 Transformers 的字段名、wrapper prefix 和部分模块名称已经变化；
- 金融模型又替换了 prediction head。

典型差异包括：

- `stride` → `patch_stride`；
- `encoder_layers` → `num_hidden_layers`；
- `encoder_attention_heads` → `num_attention_heads`；
- `w_p` → `embedder.input_embedding`；
- 旧 BatchNorm 子层名称与当前实现不同；
- `module.`、`base_model.`、`model.` 等 wrapper prefix。

### 12.2 为什么不能直接 `load_state_dict(strict=False)`

`strict=False` 只表示“不因 mismatch 抛异常”，不表示迁移成功。最危险的情况是：

- 命令能运行；
- 大部分 encoder 参数没有加载；
- 模型实际接近随机初始化；
- 研究报告却把它标成 E2 迁移学习。

这类错误比显式 crash 更难发现。

### 12.3 最终设计

第一步：配置规范化。

- 把旧字段和新字段映射到 `CanonicalPatchTSTConfig`；
- 明确检查 context length、channel 数、patch、hidden layers、attention、normalization 等；
- 架构不一致时训练前阻断。

第二步：checkpoint → 当前 Transformers source backbone。

- 规范化 state key；
- 按“规范化名称 + exact shape”匹配；
- 计算 loaded parameter numel ratio；
- 未在 allowlist 中的 missing、unexpected、shape mismatch 全部阻断。

第三步：source backbone → 金融 Alpha backbone。

- 再做一次同样的严格迁移；
- 比较完整参数和 buffer 指纹；
- 要求迁移后 target 指纹等于 source；
- 要求迁移后 target 不等于随机初始化。

代码证据：

- [`src/facdigger/models/patchtst_adapter.py`](../src/facdigger/models/patchtst_adapter.py)
- [`src/facdigger/models/patchtst_transfer.py`](../src/facdigger/models/patchtst_transfer.py)
- [`tests/unit/test_patchtst_adapter.py`](../tests/unit/test_patchtst_adapter.py)
- [`tests/unit/models/test_patchtst_transfer.py`](../tests/unit/models/test_patchtst_transfer.py)

### 12.4 面试讲法

> 迁移学习最容易出现“代码不报错，但根本没迁移”的假成功。我没有依赖 `strict=False`，而是做了两段可审计迁移：旧 checkpoint 先装进当前库的同构 backbone，再迁到金融模型。每段都按规范化 key 和精确 shape 匹配，设置参数量比例门槛，并用全模型指纹证明 target 真正等于 source、且不再是随机初始化。

---

## 13. 缺失值不能只做“补零”

### 13.1 证据与问题

- 证据等级：`工程验证暴露`
- 日频金融特征天然会缺失：新股历史不足、成交量缺失、停牌、rolling window 未成熟。

如果只把 NaN 补成 0：

- 0 可能是合法的收益或标准化值；
- pooling 会把缺失 patch 当作真实观测；
- masked reconstruction 会尝试重建人工填充值；
- 模型可能学到数据管道的缺失模式，而不是价格行为。

### 13.2 最终设计

- window 同时输出 `values` 和 `observed_mask`；
- 缺失值可补 0，但 mask 保留真实观测语义；
- encoder adapter 从 element mask 推导 patch mask 和 channel mask；
- AlphaHead 只在有效 patch 上池化；
- E3 loss 只聚合“被随机 mask 且原本真实 observed”的元素；
- 如果一个 batch 没有有效重建目标，直接报错。

代码证据：

- [`src/facdigger/datasets/window.py`](../src/facdigger/datasets/window.py)
- [`src/facdigger/models/patchtst_alpha.py`](../src/facdigger/models/patchtst_alpha.py)
- [`src/facdigger/models/patchtst_pretrain.py`](../src/facdigger/models/patchtst_pretrain.py)

### 13.3 面试讲法

> 补零只是数值层面的处理，不是语义层面的处理。我让 observed mask 从数据窗口一直传到 patch pooling 和 reconstruction loss。尤其在 E3 中，只有“原本观测到、后来被随机遮挡”的元素才进入 loss，人工补零永远不是重建目标。

---

## 14. Outer validation 不能参与 early stopping

### 14.1 证据与问题

- 证据等级：`研究审查发现`
- 早期常见训练模式是每个 epoch 看 validation loss 并保存 best checkpoint。

在单次工程实验里很常见，但在本项目的 Walk-forward 比较中，outer validation 用来比较 E0–E3。如果又用它选择 epoch，就已经把比较集用于训练决策。多模型、多 seed、多次迭代后会逐步过拟合 validation。

### 14.2 另一个更隐蔽的泄漏：标签窗口重叠

即使按 `asof_date` 把 Train 尾部 10% 切为 inner selection，前一段最后几天的 `label_end` 仍可能落进 selection 区间。这样 fitting rows 的标签使用了 selection 时间段的价格。

### 14.3 最终设计

- official Train 内部再按日期切尾部 10% 为 `inner_selection`；
- fitting rows 只来自更早的 inner fit；
- purge 所有 `label_end >= inner_selection_start` 的 fitting rows；
- early stopping 和 best checkpoint 只看 inner selection；
- outer validation/test 仅在训练结束后评价；
- manifest 记录 outer validation/test 的训练访问和 checkpoint-selection 访问行数，必须为 0；
- E3 虽然是无监督预训练，也只允许使用 official Train，因为未来分布本身也是信息。

代码证据：

- [`src/facdigger/training/common.py`](../src/facdigger/training/common.py)
- [`src/facdigger/datasets/splits.py`](../src/facdigger/datasets/splits.py)
- [`tests/unit/training/test_common.py`](../tests/unit/training/test_common.py)
- [`tests/unit/training/test_e3_split.py`](../tests/unit/training/test_e3_split.py)

### 14.4 面试讲法

> 我把“validation”拆成了 inner selection 和 outer validation。inner selection 在 Train 内，只负责 early stopping；outer validation 只负责模型比较。切分时不只看 as-of date，还 purge 掉 label end 跨过边界的样本。甚至 E3 的无监督预训练也不能读取未来 validation，因为未来的分布结构本身就是泄漏。

---

## 15. 训练恢复不是“重新加载 model.pt”

### 15.1 证据与问题

- 证据等级：`工程验证暴露`
- 训练可能运行数小时或更久，必须支持中断恢复。

只保存模型参数会丢失：

- optimizer momentum；
- scheduler 位置；
- GradScaler 状态；
- Python、NumPy、Torch、CUDA RNG；
- 当前 epoch/global step；
- early stopping 历史；
- sampler 顺序和位置。

恢复后虽然“能继续”，但训练轨迹已不同，无法判断 resumed run 是否与不中断运行等价。

### 15.2 最终设计

E1–E3 checkpoint 保存：

- model；
- optimizer；
- scheduler；
- GradScaler；
- epoch/global step；
- best epoch/loss/stale epochs；
- 完整 history；
- RNG states；
- sampler state；
- dataset ID；
- 完整 training protocol hash；
- E2/E3 的 source weights hash；
- 阶段审计。

恢复前检查：

- checkpoint family 正确；
- dataset ID 一致；
- protocol/config hash 一致；
- source weight hash 一致；
- run 状态允许恢复；
- 不能恢复已完成 run。

代码证据：

- [`src/facdigger/training/e1_engine.py`](../src/facdigger/training/e1_engine.py)
- [`src/facdigger/training/e2_engine.py`](../src/facdigger/training/e2_engine.py)
- [`src/facdigger/training/e3_engine.py`](../src/facdigger/training/e3_engine.py)
- [`tests/integration/test_e1_resume.py`](../tests/integration/test_e1_resume.py)
- [`tests/integration/test_e2_resume.py`](../tests/integration/test_e2_resume.py)
- [`tests/integration/test_e3_resume.py`](../tests/integration/test_e3_resume.py)

### 15.3 面试讲法

> 我把 resume 定义成“恢复状态机”，不是“加载模型继续跑”。checkpoint 包括 RNG 和按日期采样器状态，恢复前还绑定 dataset、完整配置和 source checkpoint hash。这样避免拿旧数据的 checkpoint 接到新协议上，生成一个表面能跑、实际不可解释的混合实验。

---

## 16. 如何证明 FT-0 真冻结、FT-1 真解冻

### 16.1 证据与问题

- 证据等级：`工程验证暴露`
- E2/E3 采用两阶段微调：
  - FT-0：冻结 encoder，只训练 alpha head；
  - FT-1：解冻 encoder 联合训练。

仅检查 `requires_grad` 不够。optimizer 参数组、错误的 `.train()` 切换、BatchNorm buffer 或代码重构都可能让实际变化与预期不同。

### 16.2 最终设计

每个阶段第一次 optimizer step 前后计算：

- encoder 参数和 buffer 的完整指纹；
- alpha head 参数和 buffer 的完整指纹。

硬门禁：

| 阶段 | Encoder | Head |
|---|---|---|
| FT-0 | 必须不变 | 必须变化 |
| FT-1 | 必须变化 | 必须变化 |

如果不符合，训练立即失败，并把阶段审计保存在 checkpoint 和报告中。

代码证据：

- [`src/facdigger/models/patchtst_transfer.py`](../src/facdigger/models/patchtst_transfer.py)
- [`src/facdigger/training/e2_engine.py`](../src/facdigger/training/e2_engine.py)
- [`src/facdigger/training/e3_engine.py`](../src/facdigger/training/e3_engine.py)

### 16.3 面试讲法

> 我没有把冻结策略当成配置声明，而是当成运行时不变量。FT-0 第一步后，encoder 指纹必须完全不变、head 必须变化；FT-1 两者都必须变化。这样能发现 optimizer 参数组配错或 buffer 被更新这类肉眼不容易看出的错误。

---

## 17. LightGBM 原生运行库冲突：用进程边界隔离

### 17.1 证据与问题

- 证据等级：`工程验证暴露`
- macOS 环境中 LightGBM、Polars 和 PyTorch 都可能加载自己的 OpenMP runtime。
- 在同一 Python 进程混合导入和运行时，可能出现冲突、崩溃或难以复现的问题。

### 17.2 最终设计

- 主流程准备标准输入文件；
- 使用独立 worker 进程训练 LightGBM；
- checkpoint 回放时也使用独立预测 worker；
- 主进程只读取标准化输出和 sidecar；
- inference 校验 model 和 preprocessing sidecar 哈希。

代码证据：

- [`src/facdigger/models/lightgbm_worker.py`](../src/facdigger/models/lightgbm_worker.py)
- [`src/facdigger/models/lightgbm_predict_worker.py`](../src/facdigger/models/lightgbm_predict_worker.py)
- [`src/facdigger/models/baselines.py`](../src/facdigger/models/baselines.py)

### 17.3 面试讲法

> 这个问题不适合靠调整 import 顺序解决，因为那很脆弱。我把 LightGBM 的原生 runtime 放进独立进程，主流程通过文件契约交换数据。代价是多一次序列化，但换来运行库隔离和独立回放能力。

---

## 18. 数据不足时，中性化不能“勉强给一个结果”

### 18.1 证据与问题

- 证据等级：`研究审查发现`
- 正式目标要求行业和 log float market cap 中性化。
- 当前 EODHD 日线没有可靠的点时行业和流通市值。

如果用今天的行业/市值回填历史，会引入未来信息；如果中性化失败后退回 raw score，却仍把列名叫 neutralized score，会产生错误研究结论。

### 18.2 最终设计

- 评价器只有在暴露存在、样本足够且设计矩阵可用时才计算 residual；
- 不满足条件时 neutralized score 保持 null；
- 记录不可用原因；
- 工程配置关闭：
  - `require_source_research_ready`；
  - `require_neutralized_positive`；
- preflight 明确标记 `research_mode=engineering`；
- 正式研究必须重新开启硬门禁，并补齐点时暴露。

代码证据：

- [`src/facdigger/evaluation/neutralization.py`](../src/facdigger/evaluation/neutralization.py)
- [`src/facdigger/research/preflight.py`](../src/facdigger/research/preflight.py)
- [`configs/research/m6_eodhd_engineering.yaml`](../configs/research/m6_eodhd_engineering.yaml)

### 18.3 面试讲法

> 我没有为了让报告完整而用静态行业或 raw score 冒充中性化结果。系统会输出空值和原因，并把研究模式降级为 engineering。这里的设计原则是：数据缺口应该降低结论权限，而不是被代码默认值掩盖。

---

## 19. 重叠收益下，普通 t 检验不可信

### 19.1 证据与问题

- 证据等级：`研究审查发现`
- 标签是 5-session forward return，相邻日期的标签窗口高度重叠。
- 日度 IC、spread 和 paired delta 因此存在序列相关。
- 如果把每天当作独立样本，普通标准误会偏小，t 值偏高。

### 19.2 最终设计

- 先按日期对多个 seed 求均值，避免把 seed 当成独立市场样本；
- fold 内计算 Newey–West / HAC 标准误；
- 另外使用固定 offset 的非重叠样本；
- 同时报告样本数、均值、标准误和 t-stat；
- 模型比较使用相同日期、相同证券键上的 paired delta。

代码证据：

- [`src/facdigger/research/statistics.py`](../src/facdigger/research/statistics.py)
- [`src/facdigger/research/aggregate.py`](../src/facdigger/research/aggregate.py)
- [`tests/unit/research/test_statistics.py`](../tests/unit/research/test_statistics.py)

### 19.3 面试讲法

> 5 日收益每天滚动计算时，相邻标签共享 4 天，不能把每一天当 IID。我同时做 HAC 和固定 offset 非重叠检验，而且先按日期聚合 seed。这样 seed 是模型随机性的重复，不会被错误地当成更多市场样本。

---

## 20. Final holdout 不能靠“大家自觉不看”

### 20.1 证据与问题

- 证据等级：`研究审查发现`
- 多模型、多 seed、多个 fold 的开发过程会反复查看 validation。
- 如果 test 也能随命令轻易运行，它最终会参与调参，失去 holdout 含义。

### 20.2 最终设计

validation 阶段结束后冻结：

- resolved research config；
- fold plan；
- 完整 cell matrix；
- 每个 validation 结果；
- 研究报告 hash。

解封 final holdout 时：

- 必须显式提供 `--unlock-final-holdout`；
- 重新校验冻结哈希；
- 只运行最后一折的 test cells；
- 已有输出不覆盖；
- test 结果与 validation 结果分层保存。

代码证据：

- [`src/facdigger/research/runner.py`](../src/facdigger/research/runner.py)
- [`src/facdigger/research/preflight.py`](../src/facdigger/research/preflight.py)
- [`tests/integration/test_m6_runner.py`](../tests/integration/test_m6_runner.py)

### 20.3 面试讲法

> Holdout 不是一个 `split="test"` 字符串，而是一套访问控制。我的 runner 先冻结 validation 配置、cell matrix 和报告哈希；只有显式解封并通过哈希复核，才运行最终 test。这样至少能让“什么时候看过 test”在工程上可追踪。

---

## 21. 训练完成不等于 checkpoint 可回放

### 21.1 证据与问题

- 证据等级：`工程验证暴露`
- 训练脚本在内存中直接预测成功，只能证明当时的 Python 对象可用。
- 它不能证明磁盘 checkpoint、预处理 sidecar、resolved config 和 dataset 能独立重建相同结果。

### 21.2 最终设计

独立 `predict` 流程：

1. 读取 source run manifest；
2. 校验 resolved config hash；
3. 校验 dataset manifest hash；
4. 校验 checkpoint hash；
5. E0 LightGBM 额外校验 preprocessing sidecar；
6. 在新流程中重建 E0–E3 模型；
7. 重新预测；
8. 默认与原始 raw score 做固定容差核对；
9. 原子生成 replay manifest、predictions、factors、metrics 和 report；
10. 不覆盖已有 replay。

代码证据：

- [`src/facdigger/inference/runner.py`](../src/facdigger/inference/runner.py)
- [`src/facdigger/evaluation/runner.py`](../src/facdigger/evaluation/runner.py)
- [`tests/unit/inference/test_runner.py`](../tests/unit/inference/test_runner.py)
- [`tests/integration/test_lightgbm_replay.py`](../tests/integration/test_lightgbm_replay.py)

### 21.3 面试讲法

> 我把 replay 当作发布门禁：训练进程退出后，用磁盘上的 manifest、配置、dataset 和 checkpoint 在独立路径重建模型，并要求分数与原输出一致。这样才能证明 checkpoint 是真正的产物，而不是训练脚本顺手保存的一份不完整文件。

---

## 22. 因子导出必须与标签彻底分离

### 22.1 证据与问题

- 证据等级：`工程验证暴露`
- 训练用 sample index 通常包含 target 和 split。
- 如果直接复用它生成“最新因子”，很容易把未来收益或评价字段一起写入生产文件。

### 22.2 最终设计

- dataset snapshot 单独生成 `inference_index.parquet`；
- inference index 不包含 target 和 split；
- 覆盖所有具备完整 context 的 eligible 日期，包括尚未形成未来收益的最新日期；
- `factors.parquet` 明确禁止 target；
- 写入：
  - raw score；
  - 可用时的 neutralized score；
  - model/checkpoint/dataset lineage；
  - `signal_available=after_close`；
  - `earliest_execution=next_session_open`。

独立 `evaluate` 命令再从不可变快照按键关联 target，而不是让 target 跟随因子文件传播。

代码证据：

- [`src/facdigger/datasets/index.py`](../src/facdigger/datasets/index.py)
- [`src/facdigger/data/snapshots.py`](../src/facdigger/data/snapshots.py)
- [`src/facdigger/inference/runner.py`](../src/facdigger/inference/runner.py)

### 22.3 面试讲法

> 我没有用训练 sample index 直接导出生产因子，而是建了一个物理上不含 target/split 的 inference index。评价时再从不可变快照按键取标签。这个边界既防止未来字段泄漏，也让最新尚无 t+5 收益的日期能够正常出信号。

---

## 23. 一些贯穿全项目的设计原则

### 23.1 Fail closed，而不是 silent fallback

适合阻断的情况：

- 主键重复；
- checkpoint hash 不一致；
- source weights 加载比例不足；
- 未授权 shape mismatch；
- outer validation 被训练访问；
- 恢复训练的数据或协议不一致；
- 因子覆盖率不足。

适合降级并标记的情况：

- 点时行业/市值不可用；
- 退市收益只能插值；
- 当前 source 只能用于 engineering 模式。

判断标准是：这个缺口会让输出“含义错误”，还是仅让输出“结论权限下降”。

### 23.2 把研究假设写进数据血缘

不仅记录代码版本，还记录：

- 股票池发现规则；
- liquidity ranking；
- adjustment basis；
- delisting imputation；
- identity quality；
- source readiness；
- 中性化可用性；
- signal timing。

### 23.3 区分工程可运行和研究可信

项目允许：

- smoke 验证接口；
- paid pilot 验证吞吐和资源；
- engineering M6 验证完整闭环。

但这些模式不会自动升级成正式研究结论。正式结论仍需要：

- 完整历史动态股票池；
- 真实或更可信的 delisting return；
- 点时行业和流通市值；
- 目标 GPU 正式门禁；
- 完整 walk-forward、多 seed 和 final holdout。

### 23.4 可观察性应该是下一轮工程重点

当前 EODHD 全量采集主要通过 cache 文件数和 budget state 观察，训练主要通过每个 epoch 写入的 `last.pt` 观察。命令行尚未持续输出：

- 当前 symbol / 总 symbol；
- cache hit ratio；
- 下载速率和 ETA；
- epoch / batch progress；
- train/inner-selection loss；
- GPU memory。

下一轮应增加结构化日志或 `tqdm`，同时保留机器可读 JSONL，避免只靠文件数监控。

---

## 24. 面试问答速记

### Q1：这个项目最难的地方是什么？

推荐回答：

> 难点不是 PatchTST forward，而是如何证明研究链路没有“看起来能跑、其实不可信”。例如真实全量 EODHD 数据出现同 ISIN 重复键时，根因同时包含 ticker alias 和被错标的权证，不能粗暴 dedup；迁移学习中 `strict=False` 也会让随机模型伪装成迁移成功。所以我把数据契约、权重审计、split 访问审计和回放哈希做成硬门禁。

### Q2：为什么不直接用 ticker 做证券 ID？

> ticker 是显示代码，不是稳定身份。公司会改 ticker，直接使用 ticker 会切断历史。项目优先使用 ISIN，同时保留 provider symbol；但 ISIN 也不能盲信，所以先过滤共享 ISIN 的衍生工具，再对真实 alias 做确定性历史合并。

### Q3：如何防止时间泄漏？

> 有三层：数据层按 known-at 和全市场 session 生成标签；训练层只在 Train 内做 inner selection，并 purge label-end 跨边界样本；研究层 outer validation 不参与 checkpoint 选择，final holdout 需要冻结哈希后显式解封。E3 无监督预训练也不能使用未来 validation 分布。

### Q4：如何证明 E2 真的使用了预训练权重？

> 不是看 `load_state_dict` 是否返回。我先把旧 checkpoint 字段和 key 规范化，再分两段加载；每段按名称和 shape 审计，计算 loaded numel ratio，拒绝未授权 mismatch。最后用全 backbone 指纹证明迁移后 target 与 source 完全一致，同时与随机初始化不同。

### Q5：为什么允许退市收益插值？

> 因为完全删除跨退市样本会形成更隐蔽、更系统性的乐观偏差。插值不是最终答案，而是显式的工程假设：逐行标记、进入 manifest、降低 research readiness，并要求敏感性分析。正式结论仍应替换为真实 delisting return。

### Q6：为什么中性化做不出来却不直接删除功能？

> 功能实现本身是有价值的，但当前 source 缺点时行业和流通市值。删除功能会丢失未来扩展接口；伪造暴露又不可信。因此 evaluator 保留中性化能力，不满足条件时输出 null 和原因，工程配置关闭硬门禁，正式配置必须补数据后重新开启。

### Q7：你如何保证实验可复现？

> dataset snapshot、resolved config、source checkpoint 和训练 checkpoint 都有内容哈希；checkpoint 保存 optimizer、scheduler、scaler、RNG 和 sampler；训练后还会在独立路径从磁盘回放并核对分数。可复现不是“seed 固定”一个开关，而是数据、状态和产物的完整绑定。

### Q8：如果重新做一次，最优先改什么？

> 第一是接入可靠的 security master 和真实 delisting return，减少规则型身份判断；第二是补点时行业和流通市值，让中性化恢复正式门禁；第三是给长时间采集和训练增加结构化实时进度；第四是在目标 CUDA GPU 上完成正式吞吐和 FP16 门禁。

---

## 25. STAR 讲述模板

### 案例 A：EODHD 身份冲突

**Situation**

全量历史采集从 smoke/pilot 扩展到 2 万多只 active + delisted 候选后，标准表唯一键检查报同一 `security_id + trade_date` 重复。

**Task**

既要恢复采集，又不能用会破坏 ticker 历史连续性的临时去重。

**Action**

检查真实 metadata 和 EOD payload，区分：

- 被错标为 Common Stock 的 warrants/units；
- 同一公司的 old/current ticker aliases。

然后实现：

- 名称、后缀、peer ticker 衍生品过滤；
- ISIN 稳定身份；
- 非重叠历史保留；
- 重叠时 active → latest coverage → symbol 的确定性优先级；
- batch 内外双重合并；
- audit 字段和回归测试。

**Result**

全项目 71 项测试通过，已有 API 缓存被复用，真实采集恢复。最终全量数据质量仍待采集结束后审计，因此不能把“进程恢复”表述成“正式实验已经成功”。

### 案例 B：PatchTST 迁移假成功

**Situation**

IBM checkpoint 与当前 Transformers 在配置字段和 state key 上存在版本差异，直接 `strict=False` 可以运行但无法证明加载了多少 encoder。

**Task**

确保 E2 和 E3 的“迁移”是可验证事实，而不是实验标签。

**Action**

- 建立 canonical config；
- 规范化历史 key；
- 两段权重迁移；
- exact-shape 和 loaded-numel 审计；
- mismatch allowlist；
- source/target/random 三方指纹验证；
- FT-0/FT-1 首步参数变化审计。

**Result**

任何未经授权的 mismatch 或加载比例不足都会在训练前失败，run 保存完整 weight load report，E2/E3 的初始化来源可以被独立复核。

### 案例 C：防止 validation 被用作训练集

**Situation**

E0–E3 需要在同一 outer validation 上比较。如果每个模型都用它 early stop，多轮开发会对 validation 过拟合。

**Task**

既保留 early stopping，又保持 outer validation 的比较效力。

**Action**

- Train 内按日期建立 inner selection；
- purge 标签窗口跨边界样本；
- checkpoint 只看 inner selection；
- outer validation/test 训练访问计数必须为 0；
- validation 完成后冻结研究哈希；
- final holdout 显式解封。

**Result**

训练选择、模型比较和最终 holdout 的职责被分离，研究报告能审计每个 split 被谁、在什么阶段访问。

---

## 26. 继续维护时的记录模板

每次遇到新问题，在本文追加以下内容，不要只记录最终代码：

```markdown
## [ID] 问题标题

- 日期：
- 阶段：
- 证据等级：实际运行故障 / 工程验证暴露 / 研究审查发现
- 触发命令或测试：
- 影响范围：

### 现象

保留脱敏后的原始错误、数据样例或错误输出。

### 根因

说明直接原因和系统性原因。

### 考虑过但放弃的方案

说明为什么没有采用最直觉的修复。

### 最终设计

说明不变量、边界、优先级和失败策略。

### 验证

列出测试、真实运行、指标或审计产物。

### 尚存局限

明确哪些问题仍没有解决。

### 面试版讲法

用 100–200 字写成可口述内容。
```

---

## 27. 当前诚实边界

截至本文更新时间，以下“已完成”指代码或历史本地资产，不表示 Git clone 自带数据或已得到
正式收益结论：

- M0–M8 工程闭环已经实现；
- 项目机器上的 EODHD 全历史 bronze 曾完成 DATA-07～11 修复并通过来源质量门禁；
- 该次审计隔离了 1,293 个无法可信归属为单一证券身份的历史，未用插值或 winsorize
  掩盖；新数据以当次 manifest 为准；
- 当前 source 的退市终值是显式插值，不是真实观察；
- 点时行业和流通市值仍缺失；
- 正式中性化 hard gate 因此关闭；
- 训练已采用共享 feature store、mmap 和同日横截面排序目标；
- M6 已要求显著性/稳健性，并在 final holdout 前执行截至 validation 末日的重新训练；
- 目标 RTX 2070 Super 上存在旧协议工程 pilot，但当前协议的完整 M6 尚未验收；
- 尚不能声称 PatchTST、迁移学习或金融预训练已经获得显著正 alpha。

面试时最可信的结论是：

> 我完成了一条强调点时语义、横截面排序、可恢复训练、严格迁移审计和研究访问控制的量化
> 因子实验平台，并在真实 EODHD 全量采集中处理了证券身份、交易日历和公司行动冲突；正式
> 收益结论仍等待真实退市收益、点时风险暴露和当前协议的目标 GPU 完整实验。

---

## 28. DATA-07～11：基础契约通过后，深审计仍发现 P0 数据污染

- 日期：2026-07-24
- 阶段：正式实验前全量数据审计与修复
- 证据等级：`研究审查发现` + `工程验证暴露`
- 数据：EODHD All World，US active + delisted，2010-01-01 至 2025-12-31
- 影响范围：市场日历、ADV20、动态 top-1000、forward label、公司行动、退市标签和
  M6 输入选择

### 28.1 为什么“契约通过”仍然不等于数据正确

第一次全量标准化完成后：

- `(security_id, trade_date)` 唯一；
- OHLC、volume、adjustment factor 数值合法；
- eligible 行具备 close、ADV20 和 rank；
- 文件哈希与 manifest 一致。

但深审计发现：

1. bars 日期并集包含交易所闭市日；
2. provider-symbol fallback 和错误 ISIN alias 中存在不同证券混合历史；
3. 公司行动在 alias 合并后仍有重复和冲突；
4. old alias 的 delisted 标志覆盖了仍 active 的 current alias；
5. M6 仍指向旧 100 股票 pilot，且旧 manifest 没有来源质量证明。

这次问题说明基础 contract 只能回答“字段和局部不变量是否合法”，无法独立回答“整条
时间序列是不是同一个证券”“这个日期是不是市场 session”以及“两个公司行动是否代表
同一个经济事件”。正式实验前必须再做跨表、跨时间和来源语义审计。

### 28.2 DATA-07：假交易日如何放大成股票池系统性缺失

旧实现从所有 bars 的日期并集生成市场 calendar。审计发现闭市日上偶尔存在 1～数根
供应商异常 bar；一根错误 bar 就会把该日扩展成所有证券的 session。ADV20 又按 20 行
窗口并要求 20 个有效值，因此一个伪 session 会让大量证券随后约 19 个真实 session 的
ADV20 变成 null。

旧数据的直接表现是：

- daily eligible 中位数只有 3；
- 812 个日期没有任何 eligible 股票；
- 首次出现 eligible 后仍有 561 个零股票日期。

最终设计没有使用“每日 bars 数低于某阈值就删除”这种依赖当前股票池规模的启发式，而
是在 provider 边界引入版本化的 `US_EQUITIES_REGULAR` 日历：

- 递归节假日：MLK、Washington's Birthday、Good Friday、Memorial Day、
  Independence Day、Labor Day、Thanksgiving、Christmas；
- 2022 年起 Juneteenth；
- Hurricane Sandy；
- Reagan、Ford、George H. W. Bush、Carter national day of mourning；
- 半日交易仍视为 session；
- New Year 落在周六时不错误地把前一周五删除。

审计初稿曾按近似规则得到 4,025 个 session，正式实现补入 2025-01-09 Carter
national day of mourning 后，2010–2025 的正确实现计数为 4,024。真实重建删除了
140 个闭市日期上的 876 根 bars；修复后 4,024 个 session 全部至少有一根市场 bar，
不存在闭市日残留。

### 28.3 DATA-08：为什么没有直接 winsorize 万倍收益

旧数据中可观察到：

- `LAN.US` 在约 0.16 与 8,000～9,000 之间切换；
- `PGS.US` 在个位数、几十和约 20,000 之间切换；
- 相同 ISIN 的不同 alias 在同一天可能给出不同价格。

如果只在 feature 层 winsorize return，错误的 raw close × volume 仍会进入 ADV20 和
top-1000，错误价格也仍会进入 benchmark、forward return 和退市终值。因此最终门禁在
alias 合并**之前**检查两类证据：

1. 同一 `security_id + trade_date` 的多个 alias，adjusted price 相差超过 5%；
2. 同一身份相邻正式 session 的 adjusted price 比值超过 10 倍或低于 1/10。

命中任一条件时整条身份被隔离；不截断收益、不修改价格、不把冲突 alias 随机选一个。
第一次只在已经合并的旧 bronze 上回放时识别出 1,093 个身份；在原始 alias 合并前执行
后又识别出额外冲突，真实重建最终隔离 1,293/13,176 个身份，即 9.81%。这也证明门禁
必须放在 consolidation 前。

项目设置 10% 系统性损失上限：若下一批来源数据需要隔离的身份比例超过该值，采集直接
失败并要求人工检查，而不是悄悄接受更大范围的数据损失。修复后的 bars 保留 11,883 个
证券、21,750,078 行；剩余相邻 session 超过 10 倍的 adjusted-price 跳变为 0。

被否决的方案：

- 直接截断 return：无法修复股票池和标签；
- 仅删除异常日：断点两侧仍可能属于不同证券；
- 所有相同 ISIN 无条件合并：真实数据已经证明供应商 ISIN 也会冲突；
- 改用 ticker 作为永久 ID：会切断正常 ticker rename 的历史；
- 无上限地自动隔离：来源格式发生系统性变化时会静默丢掉大部分市场。

### 28.4 DATA-09：公司行动冲突为什么选择“整组不用”

旧公司行动表虽然数值合法，但存在 exact alias copies，以及同一
`security_id + ex_date + action_type` 下 cash amount 或 split factor 不一致的事件。

最终处理顺序是：

1. 对相同经济条款的 alias copies 去重；
2. active alias 优先，其次按 known-at 和 provider symbol 确定性选择审计行；
3. 若同一事件 key 的经济条款不一致，整组排除；
4. 已被价格身份门禁隔离的证券，其公司行动也一并排除。

“整组排除”会损失少量事件，但优于选择一个无法证实的 dividend 或 split。重建后公司
行动为 162,302 行；事件 exact duplicate groups 和 event conflict groups 均为 0。
manifest 记录：随隔离身份删除 12,773 行、exact alias 删除 808 行、94 个冲突组
（188 行）整组删除。

### 28.5 DATA-10：退市生命周期不能用布尔值 `max()`

旧实现对同一稳定身份的 alias 使用：

```python
is_delisted_source.max()
```

只要 old ticker 被标记为 delisted，即使 current ticker 仍 active，整个身份也会被标记
为 eventually delisted。随后退市表又可能从 old alias 取较早 last trade，导致 bars 与
delistings 不一致。

修复后的优先级是：

- 任一 current alias 明确 active，则稳定身份不因 old alias 被判退市；
- 只有所有合并 alias 都是 delisted 时，才进入退市生命周期；
- `last_trade_date` 从合并后的 universe 生命周期取得，而不是过滤 old alias bars；
- delist date 使用最后交易日后的第一个正式 market session。

真实重建后 delistings 为 6,588 行，`last_trade_date` 与 bars 最后日期不一致数量从 4
降为 0。退市收益仍是交易所惩罚插值，不因此升级为正式研究数据。

### 28.6 DATA-11：让旧数据无法“误通过”

只修 provider 不够：旧 bronze 文件仍然存在，而且基础 schema contract 会通过。为避免
用户或自动化流程误建快照，新增三层来源质量证明：

1. ingestion manifest 写入 calendar 名称/版本、过滤数量、隔离身份、公司行动处理和
   `quality.gate`；
2. provider 将专属质量结果转换为版本化 `facdigger.standard_parquet` 通用证明；
   `data validate` 和 dataset snapshot 要求证明 `status == passed`，并逐文件核对
   SHA-256，防止质量证明与被替换的 Parquet 脱钩；
3. M6 preflight 独立检查同一通用证明，即使工程模式关闭 `research_ready` 硬要求，也
   不能绕过基础数据质量。下游去除供应商分支的后续修复见 DATA-12。

数据集名从 `eodhd_us_historical_liquid_v1` 升为 `v2`，使旧快照语义失效；M6 base
dataset 同时从旧 `eodhd_all_world_pilot.yaml` 修正为
`eodhd_historical_liquid.yaml`。

### 28.7 验证

代码与配置证据：

- `src/facdigger/data/providers/eodhd/market_calendar.py`
- `src/facdigger/data/providers/eodhd/quality.py`
- `src/facdigger/data/providers/eodhd/mapper.py`
- `src/facdigger/data/providers/eodhd/provider.py`
- `src/facdigger/data/adapters.py`
- `src/facdigger/research/preflight.py`
- `configs/data/eodhd_historical_liquid.yaml`
- `configs/datasets/eodhd_historical_liquid.yaml`
- `configs/research/m6_eodhd_engineering.yaml`

自动测试覆盖：

- recurring/ad-hoc closure 与半日 session；
- 非 session bar 删除；
- ticker 混合历史隔离；
- active alias 不产生假退市；
- 公司行动 exact duplicate 和 conflict；
- 缺少通用标准化证明的旧 manifest 被 validate/snapshot/preflight 拒绝。

真实缓存重建：

- 57,101/57,101 请求命中本地缓存，网络请求 0；
- 4,024/4,024 正式 session 有市场观测；
- daily eligible 中位数从 3 恢复到 1,000；
- 3,773 个 session 达到 top-1000；
- 初始 251 个 session 因 `min_listed_sessions=252` 合理为 0；
- 2010-12-31 首次出现 eligible 后，零股票 session 为 0；
- 公司行动 exact duplicate groups = 0；
- 公司行动 event conflict groups = 0；
- delisting last-trade mismatch = 0；
- `facdigger data validate` 通过。

### 28.8 尚存局限

- 规则日历不是外部交易所 calendar 服务；新增临时休市时需升级
  `CALENDAR_VERSION` 和特殊闭市表；
- 1,293 个身份采用整条隔离，工程上安全但会损失其中部分本可通过 security master
  拆段恢复的历史；
- 10 倍 adjusted-price 阈值是来源质量门禁，不是经济学上的涨跌停定义；
- ISIN 和 active/delisted metadata 仍来自 EODHD，不能替代带有效期的机构级
  security master；
- 公司行动冲突选择不用，而不是恢复真实事件；
- 真实 delisting return、点时行业和流通市值仍缺失，`research_ready` 保持 false。

### 28.9 面试版讲法

> 全量数据第一次通过 schema、主键和 OHLC 契约后，我没有直接训练，而是继续审计每日
> 股票池，发现 daily eligible 中位数只有 3。根因是供应商在闭市日偶尔给出几根 bar，
> 而 bars-union 日历把一根异常放大成全市场 session，继续污染 20 日 ADV。进一步还
> 发现 ticker 复用、错误 ISIN alias、公司行动冲突和假退市。我把门禁前移到 alias
> consolidation 之前，引入版本化交易日历、整身份隔离、冲突事件 fail closed，并让
> manifest 成为 validate、snapshot 和 M6 preflight 的强制质量证明。用 57,101 个缓存
> 响应零网络重建后，4,024 个 session 全覆盖，eligible 中位数恢复到 1,000，重复公司
> 行动和退市 last-trade mismatch 都降为 0。

## 29. DATA-12：来源质量门禁不能反向污染 provider-neutral 边界

### 29.1 问题

DATA-11 为防止旧 EODHD bronze 误通过，在 `data.adapters` 和 `research.preflight` 中直接
判断了 `provider=eodhd`、`selection.mode=historical_liquid` 与
`quality.gate.status`。门禁本身必要，但实现让下游知道供应商名称和专属 manifest 结构，
与“provider 之外只消费标准契约”的架构目标冲突；增加第二个供应商时还会继续堆积分支。

### 29.2 修复

新增 `src/facdigger/data/provenance.py`，定义版本化的
`facdigger.standard_parquet` v1 标准化证明：

- provider 负责完成供应商专属质量判断；
- 成功标准化后写入通用 `status`、`research_ready` 和标准表 SHA-256；
- `data.adapters`、训练 provenance 和 M6 preflight 只解析这一通用契约；
- 下游不再按 `eodhd` 或 `historical_liquid` 分支；
- contract 缺失、版本不支持、状态失败或表哈希不一致时统一 fail closed。

EODHD ingestion manifest schema 从 1 升至 2。旧 bronze 不包含通用证明，会被明确拒绝并要求
重新 ingest；现有快照因 source manifest 输入哈希变化而得到新的 dataset ID，不与旧产物
静默混用。

### 29.3 工程门禁与文档职责

同时新增 GitHub Actions CI，在 Python 3.10–3.12 上使用冻结 `uv.lock` 安装全部 extras，
运行 Ruff 和完整 pytest。workflow 使用只读权限，并把第三方 Actions 固定到完整 commit
SHA。

文档职责拆分为：

- `AGENTS.md`：agent 每次任务都必须知道的项目边界和最短命令；
- `CONTRIBUTING.md`：人类开发流程、风险分级、CI 与评审；
- `SECURITY.md`：秘密、外部数据和研究完整性问题；
- README/开发文档/实验设计：使用、架构和研究协议。

这避免把所有组织流程继续堆入 agent prompt，同时让可机械执行的规则进入 CI。

### 29.4 验证与限制

回归测试覆盖通用契约缺失、表哈希篡改、preflight fail-closed、EODHD 契约生成和内容寻址
snapshot。CI workflow 的 YAML、冻结安装和三版本矩阵需要在 GitHub-hosted runner 首次
运行后确认真实耗时与平台依赖兼容性。

2026-07-27 使用当前 provider 和已有缓存重新执行真实 EODHD ingestion：

- manifest 已升级为 schema 2；
- `facdigger.standard_parquet` v1 状态为 `passed`；
- bars、universe、corporate actions 和 delistings 均写入并通过 SHA-256 证据核验；
- `facdigger data validate` 通过；
- M6 engineering preflight 返回 `ready=true`、`blockers=[]`；
- `research_ready=false` 保持不变，因为退市收益仍为插值且缺少点时行业/流通市值。

## 30. TRAIN-07：多 split 重复物化全量特征导致 E3 内存放大

### 30.1 问题与根因

全量数据审计发现，`SnapshotWindowDataset` 的每次实例化都会重新把完整 features 转成
按证券组织的 NumPy values、observed mask、Python date list 和 date-position
dictionary。E1/E2 同时创建 3 个 split view，E3 创建 5 个，因此相同的约 2,240 万行
特征在一个进程中被完整复制多次。

根因不是 RTX 显存或模型规模，而是 dataset view 同时承担了“不可变特征存储”和“某个
split 的样本索引”两种职责。按修复前结构估算，E1/E2 可能超过 16 GB，E3 可能超过
24–32 GB，训练可能在首个 batch 前就因系统 RAM 耗尽。

### 30.2 修复

新增进程内共享的 `SecurityFeatureStore`：

- 每个 E1/E2/E3 run 只从 snapshot features 构造一次 values 和 observed mask；
- train、inner selection、pretrain selection 和 evaluation dataset 只保存自己的样本行
  与紧凑 location；
- 日期使用 `datetime64[D]` 并用二分查找定位，删除每行一个条目的 Python dictionary；
- location 使用两个 `int32` 数组，而不是 Python `(security_id, start)` tuple list；
- store 构造后从 `frames` 中移除原始 features DataFrame，避免训练稳定态继续保留该引用。

该设计保持 snapshot、split、标签和 checkpoint 外部契约不变，只改变进程内存布局。

### 30.3 验证与限制

新增回归测试证明多个 split dataset 引用同一个 store，并验证窗口边界、缺失 mask 和目标
不变。E1、E2、E3 resume 集成测试全部通过；完整 pytest 为 79 项，Ruff 通过。

TRAIN-07 完成时仍存以下限制；其中窗口范围裁剪和 mmap 后续已由 TRAIN-08 处理，保留这段
用于说明为什么第一次优化仍不足：

- store 首次构造仍会对 features 排序和分组，存在一次性峰值；
- 当时尚未使用 mmap 或 Arrow row-group lazy reader；
- 当时 RTX 2070 Super 的 CUDA/FP16、真实峰值 RAM 和吞吐仍待目标机实测；
- 当时建议至少 32 GB RAM，并要求完整 M6 前先跑单 cell。

### 30.4 面试版讲法

> 模型只有约 131 万参数，但全量训练的首要风险不是 8 GB 显存，而是 Dataset 为每个 split
> 重复物化约 2,240 万行特征。E3 会复制五份，可能在首个 batch 前就耗尽 32 GB RAM。
> 我把不可变特征存储和 split view 分离，让所有阶段共享一个 store；同时用
> `datetime64` 二分定位和 `int32` location 替代 Python 日期字典和 tuple list。这样没有
> 改变快照或实验协议，却移除了主要的系统内存放大因素。

## 31. ENV-01：项目虚拟环境绑定临时解释器后失效

本机 `.venv/bin/python` 曾指向 `/private/tmp` 下由 uv 下载的解释器；临时目录清理后，
入口仍存在，但 Python 启动时因标准库路径失效报 `No module named 'encodings'`。这不是
项目依赖冲突，修复方式是删除环境状态并从 `uv.lock` 重建，而不是修改依赖或复制旧
`.venv`。

2026-07-27 使用 Python 3.11.15 重新创建环境并执行 `uv sync --frozen --all-extras`；
`uv lock --check`、`facdigger doctor`、Ruff 和完整 pytest 均通过。迁移到 Windows/WSL2
时只复制代码、锁文件和数据产物，始终在目标机重建 `.venv`。

## 32. TRAIN-08：16 GB 目标机 OOM 暴露训练全流程的剩余峰值

### 32.1 真实现象与定位

共享 `SecurityFeatureStore` 解决了同一 run 内多 split 重复复制，但 RTX 2070 Super /
16 GB RAM 目标机仍在正式训练前后出现系统内存不足。这个现象说明 TRAIN-07 只处理了
稳定态重复副本，没有覆盖数据进入 store 之前的全流程峰值。

对真实 snapshot 做静态审计后得到：

- `features.parquet` 有 22,399,952 行；
- eligible sample 有 3,773,000 行；
- E0 的 7 个通道、5 个窗口、4 类统计加 last 共生成 147 列；
- 修复前 E0 在全部 feature rows 上一次性生成 Float64 统计，仅数值区理论占用约
  24.5 GiB；
- eligible E0 表和“标准化值 + missing mask”矩阵分别约 4.1 GiB，压缩 NPZ、父进程
  数组与 LightGBM 子进程还会形成重叠副本；
- E1–E3 虽共享 store，仍先读取完整 features 和无关 inference index，并在全表排序、
  `partition_by`、原始 Polars 表与 NumPy store 共存时形成一次性峰值；
- snapshot build 同时持有 bars、universe、raw/scaled features、labels 和 indexes。

因此根因是工程内存生命周期和全表宽化，不是 EODHD 行情本身质量差，也不是 131 万参数
模型或 8 GB 显存直接导致。

### 32.2 修复设计

本轮将 P0 修复拆成三条互相独立的内存边界：

1. 训练读取边界
   - `load_training_snapshot` 不再读取训练完全不用的 `inference_index`；
   - E0–E3 先确定 train-fit、inner-selection、pretrain 和 evaluation 样本，再按
     `security_id + [min(feature_start), max(asof_date)]` 扫描必要 feature rows；
   - Parquet scan 只投影配置通道及 observed mask；
   - 已排序结果直接进入 store，避免第二次全表排序；按证券迭代替代一次性
     `partition_by` 列表；
   - split dataset 只保留定位和评价所需列。目标自由的 inference dataset 显式保留
     `eligible`、行业和市值字段，防止低内存裁剪破坏中性化。

2. E0 宽表和模型边界
   - 多尺度 rolling 改为逐证券计算，并在 join 后只保留实际 sample dates；
   - 147 个统计列使用 Float32；
   - preprocessor 通过 Polars 列聚合拟合，不先创建完整 Float64 矩阵；
   - 变换逐列写入单个 Float32 目标，不再同时保留 values、normalized、mask 和 concat；
   - LightGBM 的 train/valid/evaluation 矩阵分别写入 `.npy`，子进程以 mmap 只读；
   - 启动子进程前释放 tabular 和 feature 表，训练完成后删除临时矩阵。

3. snapshot 构建边界
   - labels、sample metadata、inference index 和 features 在完成审计并写入临时目录后
     立即释放；
   - 保持内容寻址 identity、manifest schema 和最终 Parquet 契约不变；
   - 仍使用临时目录加原子 rename，失败不会留下看似完整的 snapshot。

### 32.3 修复中发现的回归

第一次裁剪 split index 时，target-free inference dataset 同时丢掉了 `eligible`、
`industry_code` 和 `log_float_market_cap`，导致最新信号中性化报列不存在。另一个改动把
E3 pretrain dataset 的创建放在共享 store 之前，触发 `UnboundLocalError`。两处问题分别
通过“推理元数据显式白名单”和“先收集所有必要窗口、再构建 store、最后构建 dataset”
修复，并加入回归测试。

这说明低内存优化不能简单地“少读几列”：必须区分监督训练最小契约和目标自由推理最小
契约，否则资源优化会改变研究语义。

### 32.4 验证、兼容性和剩余风险

回归测试覆盖：

- 必要 feature 列和证券/日期范围投影；
- 训练 snapshot 不读取 inference index；
- 多 split 共享 store 和窗口边界；
- inference dataset 不含 target 但保留中性化元数据；
- E0 统计为 Float32、preprocessor 可写 mmap `.npy`；
- LightGBM checkpoint 训练/回放；
- E0–E3 pipeline、E1 最新信号和 E1–E3 resume。

外部 snapshot、预测、checkpoint 和 CLI 契约未改变。Float32 只改变 E0 新 run 的内部统计
精度；旧 checkpoint 回放继续按其 manifest 重建相同列定义，但正式研究比较不应混用修复
前后 run，应重新运行同一版本的 E0–E3。

尚未在 RTX 2070 Super / 16 GB RAM 上测得真实 peak RSS，也未处理 sampler 的 Python
索引列表和 M6 同进程连续 cell 的 allocator 保留。这两项属于下一阶段 P1：如果单 cell
通过而完整 M6 随 cell 累积 OOM，应优先改为每个 cell 独立子进程；如果首个 E1 在 store
构造后仍接近上限，再压缩 sampler 索引表示。

### 32.5 面试版讲法

> 我先修复了 dataset 为每个 split 复制全量特征的问题，但 16 GB 目标机仍 OOM，于是按
> 对象生命周期重新审计全流程。最大问题其实是基线模型在 2,240 万行上生成 147 列
> Float64 rolling 特征，数值区本身就约 24.5 GiB；同时训练器还读取无关日期并让
> Polars、NumPy 和 LightGBM 子进程矩阵重叠。我把计算边界缩到实际样本窗口，E0 改为
> 逐证券 Float32，LightGBM 用 mmap 文件传递，snapshot 产物写盘即释放。优化中还捕获了
> 推理元数据被误裁剪的回归，所以最终把“监督训练最小契约”和“无标签信号最小契约”分开
> 定义。这样既降低峰值，又没有通过删门禁或改变 point-in-time 语义来换取跑通。

## 33. TRAIN-09：训练目标与研究指标不一致——Huber 不能代表横截面因子排序

### 33.1 真实问题

原监督训练把每只股票的 5 日超额收益当连续数值做 Huber 回归，但最终研究结论使用逐日
Spearman Rank IC。两者不完全一致：收益幅度离群值仍会影响回归梯度，而因子研究只关心
同一天股票的相对次序。进一步审计还发现，旧 `DateGroupedBatchSampler` 会把两个较小日期
拼进同一 batch；若只替换 loss，就会错误计算跨日期相关性。Checkpoint 也仍按
inner-selection Huber loss 选择，无法证明选中的是排序能力最强的模型。

E0 还有额外混杂：正式基线 LightGBM 使用 Huber，而 E1 改成排序目标后，`E1−E0` 会同时
包含模型结构和目标函数差异，不能再解释为架构增量。

### 33.2 修复设计

- 完整日期内对 target 做 average-tie rank，并映射到 `[-1, 1]`；
- 神经监督阶段优化 `1 - corr(score, target_rank)`，预测 score 不做不可导硬排序；
- 相关性计算退出 FP16 autocast，以 Float32 和显式 epsilon 保证数值稳定；
- 日期采样器均衡切分大横截面，禁止跨日拼批、丢弃余数或超过配置 batch size；
- 每轮在完整 inner selection 上计算真实逐日 Spearman Rank IC，并最大化日期等权均值；
- E0 MLP 使用相同代理；LightGBM 改为日期 group 的 LambdaRank，并用同一 Rank IC 指标早停；
- E3 仅下游微调改变目标，masked financial reconstruction 继续使用 Huber/MSE；
- checkpoint schema 升到 v2，记录 objective、target transform、selection 日期覆盖和 score
  标准差；旧 Huber checkpoint 禁止 resume 到新协议。

### 33.3 为什么不直接 soft-rank

Soft-rank/NeuralSort 更接近可微 Spearman，但通常需要额外 CUDA 扩展或 `O(B²)` 中间量，
在 Windows + RTX 2070 Super 上增加安装和显存风险。当前相关性代理是 `O(B)`、无新依赖，
并把真实 Spearman 留给 checkpoint 与正式评价。Pairwise logistic 可作为后续消融，但它更
接近 Kendall/成对一致率，还会对极小收益差施加 margin，不作为本轮默认目标。

### 33.4 回归验证与兼容性

测试覆盖 perfect/reverse ordering、平移缩放不变性、ties、非有限/零方差、日期纯净均衡
分块、E1/E2 中断恢复、E3 重建与下游目标分离，以及 LightGBM 分组训练和 checkpoint
回放。该修复不要求重新下载 EODHD 或改变已有 immutable snapshot，但所有 Huber 监督训练
结果均属于旧实验协议；正式 E0—E3 与 M6 必须在新 research ID 下重新训练。

### 33.5 面试版讲法

> 我发现模型最后用 Rank IC 做结论，训练和早停却都在优化 Huber，这会让收益幅度异常值
> 主导梯度，也让“架构增量”混入目标函数差异。更隐蔽的是旧 sampler 会把两个小日期拼
> 在一起，直接换相关性 loss 反而会计算跨日 IC。我把 target 在完整日期内转成 average
> rank，训练用 Float32 的可微相关性代理，batch 严格保持单日，checkpoint 再用完整日期的
> 真实 Spearman 选择；同时把 LightGBM 改成分组 LambdaRank。这样训练、模型选择和最终
> 因子评价终于围绕同一个横截面排序问题，且没有引入 soft-rank 的额外 CUDA 依赖。

## 34. EVAL-03：“均值为正”不是统计证据，显式 unlock 也不应绕过 no-go

### 34.1 真实问题

M6 原门禁只判断 paired Rank IC 均值方向和正 cell 比例。HAC t-stat 虽然写进报告，却没有
p-value 或显著性判定；同时 E1−E0、E2−E1、E3−E2 三个归因问题各自查看正负，会累积多重
比较假阳性。更危险的是 final holdout 只要求哈希一致和显式 unlock：即使 validation 明确
`no_go`，用户仍能启动 test cell，test 迟早会被用来继续选择方案。

另一个静默风险来自日期处理：配对代码对左右模型取日期交集，并按“实际出现的 seed”求
均值。少一天或少一个 seed 不会失败，只会缩小样本；报告中的显著性因此可能建立在不完整、
且不同模型不完全同源的观测上。

### 34.2 修复设计

- 逐 cell 核对 `predictions.parquet` 与 daily Rank IC：日期必须完全一致，逐日 `n` 必须
  等于预测横截面行数，值必须有限；
- 同 fold 的四个模型和全部 seed 必须使用完全相同的预测键，不同 fold 日期不得重叠；
- 每个 fold/date 必须恰好聚合配置中的全部 seed，配对禁止取交集降级；
- HAC 使用预注册的单侧假设 `H0: mean <= minimum effect`，输出 p-value、置信下界和
  `estimable`；长期方差非正或标准误为零时 fail closed；preflight 还要求 HAC lags 不短于
  标签 horizon；
- 三个归因问题组成固定 Holm family，控制 family-wise error；E3 raw 和 E3−E1 是总体
  主决策，分别要求单侧 HAC 显著；
- 固定 offset 非重叠样本继续做稳健性方向检查，并要求每折最低样本数；
- 20 bps Q5−Q1 保留报告但退出排序目标硬门禁；
- freeze 记录 validation 状态和 holdout eligibility。解封时先验证报告哈希，再检查
  eligibility；`no_go` 在任何 test executor 调用前抛出专用错误，且保持 manifest 为
  `validation_complete`。

`research.json` 因此升为 schema v2；当时 `freeze.json` 升为 v2，随后 final-refit 协议又将
freeze 升为 v3。旧 freeze 没有完整资格或 refit 证明，按 fail-closed 原则不能在新代码中
继续解封，必须用新的 research ID 重跑 validation。

### 34.3 为什么零方差正序列也不能直接通过

合成数据或损坏的评价流程可能每天重复同一个正 Rank IC，此时均值方向很好看，但长期方差
为零，标准误和有限 t-stat 都没有可解释性。把标准误当零会制造“无限显著”。本项目明确将
这类结果标记为 `estimable=false`，要求用真实变化的时间序列重新评估，而不是给它特殊通行。

### 34.4 面试版讲法

> 我发现研究报告虽然算了 HAC t-stat，决策却只看均值是否为正，相当于把描述统计冒充
> 推断统计；三个增量问题还存在多重比较。更隐蔽的是 paired code 会静默取日期交集，少日
> 或少 seed 的模型反而可能更容易通过。我把预测日期、逐日样本数和 seed 完整性做成 fail-
> closed 契约，主检验改为预注册阈值上的单侧 HAC，三个归因问题做 Holm 校正，非重叠样本
> 做稳健性复核。最后让 validation `go` 成为 holdout 解锁的必要条件，显式 unlock 只表达
> 用户意图，不能覆盖统计 no-go。

## 35. RESEARCH-02：Final holdout 前必须重新训练，不能复用两年前的模型

### 35.1 真实问题

原 M6 在 validation 冻结后直接复用 wf3 snapshot 跑 test。wf3 的边界是 Train 截止
2022-12-30、Validation 截止 2024-12-31、Test 截止 2025-12-31，因此 test cell 的 scaler
和模型权重仍只使用到 2022 年。2023—2024 已完成模型比较，却没有进入最终拟合；这既浪费
最近两年数据，也让 2025 holdout 结果混入不必要的模型陈旧效应。

直接把原 valid 行并入现有 snapshot 也不正确：原 scaler 只在 2022 年前拟合，而且 snapshot
是内容寻址不可变产物。更不能为了“用满数据”在 2025 test 上早停或选 checkpoint。

### 35.2 修复设计

- validation freeze 预先记录 final-refit protocol，而不是等解锁后临时决定边界；
- 只有 `overall_e3=go`、freeze/report 哈希一致且显式 unlock 后才执行 refit builder；
- unlock 获准后、refit builder 第一次核对 test 键之前就把
  `holdout_has_been_read=true` 写入 freeze；即使随后中断，也不能把 holdout 重新伪装成未读；
- 新 snapshot 使用 `train_end = valid_end = 2024-12-31`。相等边界明确表示 outer validation
  为空，test 仍在 5-session embargo 后开始；普通 M6 folds 继续要求严格
  `train_end < valid_end`；
- scaler 在扩展 Train 上重新拟合，E0—E3 复用冻结配置，并继续在 official Train 内部做
  selection 和 label-overlap purge；2025 数据不进入训练或 checkpoint 选择；
- 新旧 snapshot 的来源文件哈希必须相同，避免 validation 后悄悄换数据版本；
- refit 前后 2025 `(security_id, asof_date, target)` 必须完全一致，防止边界调整改变 holdout；
- 12 个 refit cells 使用新 dataset ID，写入 `matrix.json.final_refit`；`final_refit.json` 和
  freeze 记录 protocol、训练截止日、新旧 dataset ID、split counts 与 manifest hash；
- refit 中断标记为 `holdout_failed`，恢复时重建同一个内容寻址 snapshot，并跳过已完成 cells；
  但跳过前必须重新校验 metrics/predictions 哈希，完成标记不能掩盖产物被修改或丢失。

### 35.3 为什么仍保留 Train 内 selection

冻结的是模型配置、数据边界和选择规则，不是某一个旧 fold 的权重。最终 refit 把 2023—2024
纳入 official Train，但各训练器仍按冻结的 selection fraction 在 Train 尾部选择 checkpoint；
这不会读取 outer validation 或 test，并保持 E0—E3 与前面 folds 使用同一种训练协议。报告
会明确记录 fit/selection 行数，不能声称每一条截至 2024 年的样本都参与了梯度更新。

### 35.4 面试版讲法

> 我审计 final holdout 时发现 runner 虽然冻结了参数，却直接拿“只训练到 2022 年”的 wf3
> 模型预测 2025，2023—2024 只用于比较、没有进入最终拟合。我没有修改 immutable snapshot
> 或把 test 当 validation，而是定义了一个冻结的 final-refit 协议：新建 train_end 与
> valid_end 同为 2024-12-31 的内容寻址快照，让 outer validation 为空，重新拟合 scaler 和
> 全部模型，同时保留 Train 内 selection。解锁后还会核对来源哈希和 2025 test 键完全不变；
> 中断时按 cell 恢复。这样 2025 才是真正由截至 2024 年信息训练出的最终样本外结果。
